[Unit]
Description=Daily daysout scrape

[Timer]
# Early morning so Saturday's events are fresh at breakfast; the random
# delay keeps the load polite if many instances ever exist
OnCalendar=*-*-* 05:30
RandomizedDelaySec=20m
Persistent=true

[Install]
WantedBy=timers.target
